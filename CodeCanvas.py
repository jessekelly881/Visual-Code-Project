from CanvasBackend import CanvasBackend
from CodeScene import CodeScene

#Makes a code scene "executable" as well as does some "meta" stuff
#This is entirely independent of CodeView
class CanvasBackend:
	def __init__(self, scene):
		super(CanvasBackend, self).__init__()



	@staticmethod
	def createFromSaveFile(file_name): pass

c = CodeCanvas(None)	